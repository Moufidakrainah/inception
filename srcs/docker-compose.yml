services:
  mariadb:
    image: mariadb
    build: ./requirements/mariadb
    container_name: mariadb
    volumes:
      - db_volume:/var/lib/mysql
    networks:
      - inception
    restart: always
    env_file:
      - .env
    secrets:
      - db_password
      - db_root_password
  wordpress:
    image: wordpress
    build: ./requirements/wordpress
    container_name: wordpress
    volumes:
      - wp_volume:/var/www/wordpress
    networks:
      - inception
    restart: always
    env_file:
      - .env
    depends_on:
      - mariadb
    secrets:
      - db_password
      - wp_admin_password
      - wp_user_password
  nginx:
    image: nginx
    build: ./requirements/nginx
    container_name: nginx
    volumes:
      - wp_volume:/var/www/wordpress
    networks:
      - inception
    restart: always
    depends_on:
      - wordpress
    ports:
      - "443:443"
volumes:
  db_volume:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/mobougri/data/mariadb
  wp_volume:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/mobougri/data/wordpress
networks:
  inception:
    driver: bridge
secrets:
  db_password:
    file: ../secrets/db_password.txt
  db_root_password:
    file: ../secrets/db_root_password.txt
  wp_admin_password:
    file: ../secrets/wp_admin_password.txt
  wp_user_password:
    file: ../secrets/wp_user_password.txt
